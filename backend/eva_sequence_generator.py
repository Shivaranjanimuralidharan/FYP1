def generate_eva_sequence(df, template):
    """
    Minimal placeholder to avoid backend errors.
    Generates a 1‑insight EVA sequence using template values.
    Replace this later with your full Module 3 EVA logic.
    """
    return [
        {
            "measure": template["measure"],
            "insight_type": template["insight_type"],
            "breakdown": template["breakdown"],
            "subspace": template.get("subspace")
        }
    ]
